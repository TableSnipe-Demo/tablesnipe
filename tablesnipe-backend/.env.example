# TableSnipe Configuration
# The secret used to gate access to the app
TABLESNIPE_SECRET=your-secret-here

# Database path (defaults to /data/app.db for deployment, or local tablesnipe.db)
# TABLESNIPE_DB_PATH=/data/app.db
